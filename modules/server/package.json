{
  "name": "@veil/server",
  "scripts": {
    "build": "cargo build",
    "dev": "watchexec -w src -e rs -r -- cargo run"
  },
  "dependencies": {
    "@veil/compositor": "workspace:*",
    "@veil/encoder": "workspace:*",
    "@veil/proto": "workspace:*"
  }
}
